// // import {authController } from "../controllers/controller.Auth";
// import { mockRequest, mockResponse } from ".";
// import postController from "../controllers/controller.Post";

// describe("readAllPostController", () => {
//   it("should not return an array of post", async () => {
//     await postController.readAllPostController(mockRequest, mockResponse);
//     expect(mockResponse.send).toEqual(200);
//   }, 20000);
// });
